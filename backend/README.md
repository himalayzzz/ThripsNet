# Backend Module

This module will contain the ThripsNet API service.

## Suggested Starter Stack
- Framework: FastAPI or Node.js/Express
- Database: PostgreSQL or MongoDB
- Auth: JWT-based authentication

## Next Steps
1. Define API contracts for disease detection, wind prediction, and notifications.
2. Create service folders: `src/`, `tests/`, and `docs/`.
3. Add environment config and deployment scripts.
