# AI Model Module

This module will hold model training, evaluation, and inference code for disease detection.

## Suggested Structure
- `training/` for training scripts
- `inference/` for prediction logic
- `weights/` for exported model artifacts
- `notebooks/` for experiments

## Next Steps
1. Choose framework: TensorFlow or PyTorch.
2. Define dataset format and label schema.
3. Add baseline model training and evaluation scripts.
